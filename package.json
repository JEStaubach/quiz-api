{
  "name": "quiz-api",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "apollo-server-lambda": "^2.14.2",
    "axios": "^0.19.0",
    "encoding": "^0.1.12",
    "graphql": "^15.0.0"
  },
  "scripts": {
    "start": "netlify-lambda serve src/lambda",
    "build": "netlify-lambda build ssrc/lambda"
  },
  "devDependencies": {
    "netlify-lambda": "^1.4.5"
  }
}
